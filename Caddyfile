:2095 {
    # 1. 节点流量
    handle_path /ws {
        reverse_proxy 0.0.0.0:32619
    }

    # 2. 暴力尝试所有可能的面板端口
    # 访问 域名/2053 试一下
    handle_path /2053/* {
        reverse_proxy 0.0.0.0:2053
    }

    # 访问 域名/2086 试一下
    handle_path /2086/* {
        reverse_proxy 0.0.0.0:2086
    }

    # 访问 域名/2096 试一下
    handle_path /2096/* {
        reverse_proxy 0.0.0.0:2096
    }

    # 3. 如果都不行，显示当前环境信息
    handle {
        respond "Caddy is LIVE. Try adding /2053/ or /2086/ to URL." 200
    }
}
