:2095 {
    # 验证测试：把外部的 /mysui/ 转发给内部 s-ui 的 /app/
    handle_path /mysui/* {
        reverse_proxy 0.0.0.0:2053
    }

    # 兜底：如果访问其他路径，返回一段话
    handle {
        respond "Caddy is here, but I won't let you see s-ui yet!" 200
    }
}
